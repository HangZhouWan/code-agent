# Code Agent — 项目说明

## 项目概述

Code Agent 是一个**角色分工型多 Agent 协作平台**，采用 monorepo 架构（pnpm workspace），全栈 TypeScript 开发。

用户以自然语言下达指令，Orchestrator 自动规划并派发任务给各角色 Agent（Code / Test / Doc），Agent 之间通过 Event Bus 互相发现、通信、协作，支持动态任务分配和中途恢复。

项目以学习为核心目的，重点关注**运行时基础设施（Harness Engineering）**：权限沙箱、Hooks 机制、上下文管理、Event Bus 通信、Checkpoint 恢复。

## 技术栈

| 维度 | 技术 |
|------|------|
| 语言 | TypeScript（全栈） |
| 运行时 | Node.js 22+，ESM |
| 包管理 | pnpm monorepo（workspace 协议） |
| LLM 框架 | LangChain.js + LangGraph |
| 后端框架 | Fastify 5 + WebSocket |
| 数据库 | SQLite (better-sqlite3) + Drizzle ORM |
| 前端 | React 19 + Vite 7 + Tailwind CSS 4 |
| 构建 | tsc + esbuild |

## 项目结构

```
packages/
├── core/      @code-agent/core   — 核心引擎（LLM 抽象、工具、Event Bus、State Manager、Agent Runtime）
├── server/    @code-agent/server — 后端服务（Fastify 网关、LangGraph Orchestrator、数据库）
├── web/       @code-agent/web    — React 前端（聊天界面、流式显示、审批卡片）
└── cli/       @code-agent/cli    — CLI REPL（全局命令、分层配置、单包发布）
```

### 五层架构

1. **Orchestrator**：Planner → Dispatcher（双通道：direct/bus）→ Replanner → Finalizer
2. **State Manager**：全局状态管理（Task / Workflow / Agent / Artifact）
3. **Event Bus**：Agent 间松耦合通信（Command 指令 + Event 通知）
4. **Agent**：分角色 Agent（Code / Test / Doc），Role → Reasoning → Runtime → Capability
5. **Agent Runtime**：Execution Engine + Checkpoint + Context + Memory（三层记忆）

### 核心设计约束

- **Harness 是唯一执行通道**：Agent 不能绕过 Runtime 直接调用工具或 LLM
- **Event Bus 是唯一通信通道**：Agent 之间不直接调用
- **State Manager 是所有状态的唯一真相源**

## 开发约定

- 使用中文进行交互
- 注释用中文，写出 function 的输入/输出以及 function 的功能
- 每个 `.ts` 文件对应自己的单元测试文件
- 单元测试覆盖率应达到或超过 90%
- 每次更改都需要通过测试
- 每次更改完成后提交 git

## 常用命令

| 命令 | 说明 |
|------|------|
| `pnpm dev` | 并行启动所有子包的开发模式 |
| `pnpm cli` | 本地启动 CLI REPL |
| `pnpm build` | 生产构建 |
| `pnpm typecheck` | 全量类型检查 |
| `pnpm -r test` | 运行所有测试 |
| `pnpm publish:cli` | 构建并发布 CLI npm 包 |

## 工作区配置

- 运行时数据存储在 `~/.code-agent/projects/<slug>/`
- 分层配置优先级：全局配置 → 项目配置 → .env → 环境变量 → CLI 标志
- LLM 提供商支持 OpenAI、Anthropic、OpenAI 兼容协议
